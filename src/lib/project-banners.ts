export const projectBanners: Record<string, string> = {
  kubevigil:
    "Lifecycle policy: objects transition to Glacier once your cluster achieves GitOps enlightenment. Drift detected in 60s or the alert is on the house.",
  eventify:
    "Object lock enabled -- double bookings are physically incapable of happening here. DynamoDB conditional writes checked twice, we promise.",
  sentinel:
    "CloudWatch alarm state: OK. We checked. Then we built a whole pipeline to keep checking, forever.",
  voxagent:
    "This bucket speaks 3+ languages fluently. Your other buckets can barely handle UTF-8.",
  "ppe-safety":
    "Bucket policy denies PutObject without a hard hat in frame. No exceptions, no appeals, 25FPS.",
};
